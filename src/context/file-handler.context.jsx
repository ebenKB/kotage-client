import React from 'react';

/**
 * sets the details on the right hand side of the file preview component
 */
const details = '';
const FileHandlerContext = React.createContext(
  details,
);

export default FileHandlerContext;
